Evitando Duplicados: Crie um array de frutas. Antes de dar o .push() em uma nova fruta, use um if com o método .includes("nomeDaFruta") para saber se ela já existe. Só adicione se ela não estiver no array.

